spring:
  application:
    name: SecurityPart2
  datasource:
    url: jdbc:mysql://localhost:8889/security-2
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false

  output:
    ansi:
      enabled: ALWAYS